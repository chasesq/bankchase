- name: Upload Playwright Report
  uses: actions/upload-artifact@v4  # Changed from v3
  if: always()
  with:
    name: playwright-report
    path: playwright-report/
    retention-days: 30
  
- name: Upload test results
  uses: actions/upload-artifact@v4  # Changed from v3
  if: always()
  with:
    name: test-results
    path: test-results/
    retention-days: 30 - name: Upload API test results
  uses: actions/upload-artifact@v4  # Changed from v3
  if: always()
  with:
    name: api-test-results
    path: test-results/
    retention-days: 30
